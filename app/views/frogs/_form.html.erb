<%= form_with(model: frog) do |form| %>
  <% if frog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(frog.errors.count, "error") %> prohibited this frog from being saved:</h2>

      <ul>
        <% frog.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
